<%= form_for(@expiree) do |f| %>
  <% if @expiree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expiree.errors.count, "error") %> prohibited this expiree from being saved:</h2>

      <ul>
      <% @expiree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
